import { Settings } from "lucide-react";
import OptionItem from "@components/controllers/components/OptionItem";

export default function SettingsButton() {
  return <OptionItem Icon={Settings} />;
}
